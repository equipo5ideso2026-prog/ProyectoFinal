# Copia este archivo a .streamlit/secrets.toml y completa tu connection
# string real de MongoDB Atlas. NUNCA subas secrets.toml a GitHub
# (agrégalo a tu .gitignore).
#
# Atlas → Connect → Drivers → copia el string "mongodb+srv://usuario:pass@cluster..."
# En Atlas, ve a Network Access y agrega 0.0.0.0/0 si vas a desplegar en
# Streamlit Community Cloud (la IP del servidor cambia).

MONGO_URI = "mongodb+srv://marianocruzchavez738_db_user:XsH1VpctVz94qEoj@cluster0.drbo2p4.mongodb.net/?appName=Cluster0"
